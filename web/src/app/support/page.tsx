import type { Metadata } from "next";

export const metadata: Metadata = { title: "Support", description: "Help using SortDock on your Mac." };

export default function SupportPage() {
  return <main className="mx-auto max-w-3xl px-6 py-16 text-[var(--ink)]"><h1 className="font-serif text-5xl">SortDock support</h1><p className="mt-6">SortDock watches one folder and sorts files using rules you choose. Need a hand? Email <a className="font-semibold text-[var(--accent-deep)]" href="mailto:dq@followusai.com">dq@followusai.com</a>.</p><section className="mt-12 space-y-7"><h2 className="font-serif text-3xl">Common questions</h2><p><strong>Folder access:</strong> Open SortDock, choose the watched folder, then choose the folder again if macOS says access needs attention.</p><p><strong>Change the watched folder:</strong> Open SortDock and select a different folder in Watch Folder.</p><p><strong>Pause sorting:</strong> Use Pause Sorting in the menu bar, then Resume Sorting when you are ready.</p><p><strong>Rules:</strong> Filename keyword rules are checked first. File-type rules are used when no keyword matches.</p><p><strong>Ask Later:</strong> This keeps the file where it is and asks again after your chosen delay.</p><p><strong>Same-name files:</strong> SortDock keeps both files and gives the new one a safe name such as <code>example (1).pdf</code>.</p><p><strong>Open the window:</strong> Choose Open SortDock from the menu-bar icon.</p><p><strong>Launch at login:</strong> Turn it off in SortDock’s behavior settings.</p><p><strong>Start setup again:</strong> Choose Run Setup Again. Your existing rules and activity stay in place.</p><p><strong>Missing menu-bar icon:</strong> Relaunch SortDock from Applications. If it is still missing, check whether it is hidden in macOS Control Center.</p><p><strong>Destination unavailable:</strong> Open SortDock and choose the destination’s reconnect action to grant access again.</p></section></main>;
}
