export type Device = "mac";
export type Orientation = "landscape";
export type Platform = "mac";
export type SlideLayout = "hero" | "window-bottom" | "window-top" | "two-windows" | "no-window";
export type ElementTransform = { x: number; y: number; width: number; height: number; rotation?: number; zIndex?: number };
export type BuiltInElementId = "caption" | "device" | "deviceSecondary";
export type TextElementId = `text:${string}`;
export type ImageElementId = `image:${string}`;
export type ElementId = BuiltInElementId | TextElementId | ImageElementId;
export type SelectedElement = { slideId: string; elementId: ElementId };
export type LocalizedText = Partial<Record<string, string>>;
export type TextElement = { id: string; text: LocalizedText; transform: ElementTransform; fontSize?: number; fontWeight?: number; color?: string; align?: "left" | "center" | "right" };
export type ImageElement = { id: string; src: string; transform: ElementTransform; fit?: "cover" | "contain"; fade?: { edge: "top" | "bottom" | "left" | "right"; amount: number } };
export type Slide = { id: string; layout: SlideLayout; label: LocalizedText; headline: LocalizedText; screenshot: string; screenshotSecondary?: string; inverted?: boolean; backgroundColor?: string; transforms?: Partial<Record<BuiltInElementId, ElementTransform>>; textElements?: TextElement[]; imageElements?: ImageElement[] };
export type ThemeId = "sortdock-navy" | "sortdock-ice" | "sortdock-night";
export type ScreenshotFontId = "template-serif" | "system-sans" | "classic-serif" | "avenir-next" | "helvetica-neue" | "american-typewriter" | "baskerville" | "optima" | "palatino" | "futura" | "self-hosted";
export type ImportedFont = { src: string; format: "woff2" | "woff" | "truetype" | "opentype" };
export type Theme = { id: string; name: string; bg: string; bgAlt: string; fg: string; fgAlt: string; accent: string; muted: string };
export type ProjectState = { schemaVersion?: number; appName: string; themeId: string; fontId?: ScreenshotFontId; importedFont?: ImportedFont; connectedCanvas: boolean; locales: string[]; locale: string; device: Device; orientation: Orientation; slidesByDevice: Record<Device, Slide[]>; appIcon?: string };
