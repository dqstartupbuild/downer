# Export compliance

The app uses Apple system frameworks and no custom or non-exempt encryption. Generated Info.plist sets `ITSAppUsesNonExemptEncryption` to `NO`. Confirm this against the final archive and App Store Connect compliance prompt.
